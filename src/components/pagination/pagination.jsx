import React from "react";

const Pagination = ({ pageLimit, totalTasks, pageNeighbours }) => {
    console.log(pageLimit, totalTasks, pageNeighbours);
    return (
        <div className='paginate'>
            <p>Hello!</p>
        </div>
    );
};

export default Pagination;
